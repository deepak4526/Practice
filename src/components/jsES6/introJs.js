import { Typography } from "antd";
const { Title, Paragraph, Text } = Typography;

const JSIntro = () => {
  return (
    <>
      <Title level={1} className="font-bold underline underline-offset-8 mb-3">
        JavaScript (ES6+) - Detailed Explanation
      </Title>
      <p className="text-lg">
        React is built on JavaScript, so a strong understanding of modern
        JavaScript (ES6+) is essential. Below, we will explore key JavaScript
        concepts that you must master before diving into React.
      </p>
    </>
  );
};
export default JSIntro;
